# XuanJian 运行时依赖（从 pyproject.toml 提取）
# 安装: pip install -r requirements.txt
# 浏览器内核需额外安装: python -m playwright install chromium

# LLM
openai>=1.30.0
anthropic>=0.30.0

# MCP
httpx>=0.27.0
mcp>=1.0.0
fastmcp>=0.5.0

# 浏览器
playwright>=1.40.0

# 代理抓包
mitmproxy>=10.0.0

# Web 服务
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.6

# OCR / 验证码辅助（可选，缺失时 OCR 功能不可用但不影响启动）
ddddocr>=1.0.5
rapidocr-onnxruntime>=1.3.0

# 基础
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
rich>=13.0.0
typer>=0.12.0
