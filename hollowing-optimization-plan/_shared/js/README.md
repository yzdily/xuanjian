# Shared JS Libraries

Place minified JS libraries here for report authoring:

- `echarts.min.js` — ECharts charting library
- `mermaid.min.js` — Mermaid diagram library

These are referenced by reports via `<script src="./_shared/js/xxx.min.js"></script>`
and copied to `_shared/js/` in the deployed output.
